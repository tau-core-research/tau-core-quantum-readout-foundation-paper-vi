# Tau Core Quantum Readout Foundation Paper VI

**Title:** Quantum Readout from Observer-Relative Lossy Descent  
**Status:** first finite proof packet; conditional foundations result

The quantum separator handoff now uses one coherent joint source
representation/state datum for the mixed ideal, multiplicity pairing, relative
P3--P4 gluing and faithful block occupation. This is conditional compression,
not a derivation of that datum from the narrow source or a proof that the
globally incomplete terminal family is gauge-invariant.

## Quantum and Hybrid Descendants

The quantum terminal is a typed descendant of the common preterminal
descriptor, not an independently sourced channel. Its phase, effect,
composition, tensor and resolution structures are retained by the quantum
terminal and quantizer. Hybrid readouts jointly retain several typed legs of
the same descriptor before final quantization. Common ancestry does not make a
contextual quantum terminal operationally identical to a classical
noncontextual gravity terminal, and Nature-wide terminal completeness remains
open.

## Atemporal Parent-Realization Terminology

Because the Tau parent is atemporal, “Nature selects” does not mean an
external agent or a later decision. It is legacy shorthand for the internal
physical implication

\[
(B_\tau^{\mathrm{phys}},s_U,\mathcal L_{\mathrm{parent}})
\Longrightarrow \mathfrak P,
\]

together with physical occupation of the support of \(\mathfrak P\). A theorem
inside a declared completion proves only class-relative realization; it does
not prove this unrestricted base–seed arrow. Agreement of finitely many
terminal readouts also cannot establish ambient-source exhaustivity. The
current terminology is therefore **parent-law realization** and **physical
occupation**.

## Observer Co-Descent

Finite observer-relative quantization participates in forming operational
readout classes on an occupied carrier, but an observer exists only with
regular local rank four and a nonzero record/effect. Observer and accessible
4D context are co-readouts. This does not make finite loss sufficient for
phase composition, Born statistics, tensor structure or no-signalling.

## Resolved Record Interface

A nonzero continuous precursor is not automatically an observer-resolved
transition. The manuscript now derives an explicit stable-cell exit window
from the complete record first jet, its quadratic remainder and source-owned
inner/outer cell radii. On a constant-rank access stratum, every nonzero
outward Kato projector jet changes at least one member of an exhaustive
complete effect--Choi instrument, although the instantaneous probability
vector and an incomplete record family may remain blind. Standard-apparatus
projection, finite-cell exit and physical joint occupation remain separate
tests.

Two exact controls now delimit this result. Direct full-support access has
`P_O=I_n` and therefore zero outward/Kato jet even if the terminal process
changes. Pure scalar `U(1)` transport changes only a common Kraus phase and is
null in normalized projective Choi orientation. Non-scalar process transport
therefore requires an independently sourced internal body connection or a
genuinely varying access subspace; neither is established by the current
multimode-fibre packet.

## History Refinement Boundary

Early and later records are treated as simultaneous Parent maps. Later data
intersect the compatible inverse-image fibre rather than rewrite a stored
Parent past. A common pre-choice preparation and a normalized later-local
instrument preserve the earlier marginal; stable-record invariance alone is
insufficient. Complementary postselected interference patterns may therefore
coexist with no retro-signalling. This is compatibility with standard quantum
mechanics, not evidence for Tau Core.

## Inherited Minimal Formation Basis

Paper VI inherits the joint-law-first upstream definition: a physically fixed
zero source defines the base response, a response-distinct base-relative
class defines the seed, and a stable selected nonneutral response defines the
body. Quantum readout results cannot provide the missing zero-source anchor or
infer physical seed occupation backward from terminal statistics.


The current physical MVP conditionally embeds density/POVM kinematics,
Born probabilities and interference, unitary evolution, singlet/Tsirelson
correlations, local-CPTP no-signalling, and standard free-algebraic plus
perturbative-causal QFT constructions under explicit field assumptions. These
are standard quantum structures recovered under the declared source packet,
not Tau-specific predictions. The proposed Tau discriminator is the
independently reconstructed same-carrier relation
`x_A=A_X^PC/A_*=d_U,chord^2=x_Q`, with a certified approximate-recovery
envelope inherited from Technical Paper III. The associated QOR timing bound
is downstream of this identity and uses a standard quantum speed limit.
No audited public carrier currently makes both axes independently observable.
Physical carrier identification, unrestricted physical occupation, a universal effect
size, nonperturbative QFT, Standard Model content, outcome selection and
quantum geometry remain open.

A separate execution-to-process census completed on 2026-08-24 found no
verified public package joining an actually executed, winding-resolved control
waveform to boundary keys, same-run shots, complete QPT/Choi reconstruction
and nuisance metadata. The public quantum-memory repository is the strongest
laboratory near-pass. This is an operational incidence-identifiability stop,
not a negative Tau result and not a failure of the complete-Choi theorem.

The upstream finite-local support theorem adds one boundary: positive
seed-unloaded split quantum candidates stabilize at zero, while stable loaded
or cross-coupled candidates belong to generated body support. It does not
supply the still-open physical source of the full quantum packet.

Paper IV's operational quotient and the joint-seed factorization theorem add
the exact one-source condition: the complete relation/state selector must be
constant on complete seed fibres. Then the quantum packet lives on the
seed-generated physical quotient and inert ambient summands are irrelevant.
The current narrow seed does not prove this condition; a non-null same-seed-key
quantum difference would falsify the one-source completion.

## Projective/body measure boundary

The Fubini--Study/Uhlmann geometry used by the quantum terminal is not
automatically the morphological body volume. CCOC supplies a rank-three
relative map and OGAS a compatible signed line, but equal total rank does not
construct the full projective/body connector. If a source-owned
split-preserving connector is additionally required to match the two declared
product measures, its common-line Jacobian is uniquely `g_FFL^-3`. This is a
conditional normalization theorem, not evidence that the connector exists or
that normalized morphology predicts the decimal SI value of `hbar` or any body
length/time unit. The common M4 clock--gravity--action frame is inherited only
up to `(L_H,T_H,A_*) -> (sigma L_H,sigma T_H,sigma^2 A_*)`; one measured
dimension-bearing anchor chooses its SI representative. This metrological
quotient does not construct the projective/body connector or fix a
dimensionless quantum coupling.

Technical Paper VII-B now supplies a sharper conditional carrier result: a
pointed exterior seed with faithful Hodge and source-fidelity/no-bypass
structure jointly occupies the three pair directions and one top-form
direction. This closes that finite support inside the enriched parent class,
but does not by itself derive Born probabilities, tensor structure,
no-signalling or unrestricted physical parent ownership.

## Braid holonomy and SMCF status

The manuscript now separates an exact conditional anyon-readout criterion
from source realization: a nontrivial braid-group holonomy must survive the
observer quantizer and terminal null quotient on an independently established
two-dimensional collision-free support. The current Tau source does not force
those ingredients.

The inherited rooted body quotient also defines a source-frozen observer
compression target. A new seed metric-copy functor (SMCF) would transfer the
body metric to every admissible observer--source module and conditionally
gives a tangential-doublet/radial-singlet stiffness ratio of `0.725200588341`.
The present packet has zero mixed body-observer Hessian and does not derive
SMCF. Existing public anyon and topological-spectroscopy data do not measure
the required calibrated 3x3 static susceptibility, so this is an untested
conditional precursor rather than empirical support.

The paper now also states the quantum-side boundary for observer-dependent
terminal mixing. A shared parent mode may appear in different terminal blocks
for different observers, but full role exchange must preserve effects,
instruments, coherent composition, probabilities and no-signalling. A
contextual quantum fragment cannot thereby become a classical noncontextual
gravity fragment. Paper VII-A owns the general joint-access theorem; Paper VI
owns the quantum operational obligations imported by that theorem.

The main paper contains a compact public-data verdict. The complete census,
countermodels and source-port audits are preserved in
`paperVI_submission_source/public_data_audit_supplement.tex`. The audit reuses
frozen vector-figure extractions from a
superconducting-qubit QGT experiment and an independent NV-centre QGT
experiment. The two platforms strongly replicate local same-carrier quantum
geometry (`r=0.9892` and `r=0.9933` for the informative comparisons). They do
not test the finite Tau identity because every available axis is a standard
quantum-geometric terminal and no independent once-counted morphological
action `x_A` is present. This is a public-data identifiability no-go, not a
negative Tau detection.

A frozen eight-candidate census identifies Ness et al. (2021) as the strongest
standard QOR control. Across 53 public overlap points, no violation of the
larger Mandelstam--Tamm/Margolus--Levitin lower limit exceeds `1.96 sigma`
(`max=1.4465 sigma`). A complementary two-band countermodel proves that
spectral curvature and state-space QGT do not determine one another in
general, so spectral data cannot be renamed as an independent `x_A` without a
separately derived source law.

An additional spectral-moment no-go shows that energy susceptibility and
fidelity susceptibility scale respectively as inverse first and inverse second
powers of excitation gaps. Even independently computed estimators therefore
cannot be identified without a source-owned gap-weighting and normalization
law.

The resulting operational no-go is explicit: the frozen public observable
algebra contains no independent estimator of `x_A`. QGT/tomography is
circular, fixed-gap reversible work can vanish at nonzero chord, band
curvature is state-geometry independent, and energy susceptibility has the
wrong spectral moment. Reopening requires a source/body-side local stiffness
law frozen before blind predictions of withheld quantum endpoints.

Equal local stiffness is itself not enough: a straight amplitude curve and a
curved unit-speed amplitude curve have the same local metric but different
finite chords. The minimal blind packet must therefore also freeze the
connector, its transport/holonomy and the common action unit.

Pairwise fidelities also fail to determine loop transport: complex-conjugate
three-state packets can have identical endpoint distances and opposite
Bargmann phases. At least one closed triangular phase/holonomy witness is
therefore mandatory.

A finite alternative-source audit adds a constructive outcome. Quantum-work
interferometry, endpoint/process tomography and mixed-state geometric or
Uhlmann phase measurements have each been demonstrated experimentally, but no
located public experiment supplies all three as one source-frozen same-carrier
packet with an independent body-action map. The preferred reopening target is
therefore one programmable qubit--ancilla carrier combining those three legs;
cross-platform stitching and relabelling thermodynamic work as morphological
action remain forbidden.

The strongest newly located partial candidate is the Naghiloo--Murch
trajectory experiment: one transmon supplies continuously reconstructed state
paths plus trajectory-resolved heat and work. It still fails the Tau hard gate
because the reported work is derived from `rho(t),H(t)`, no closed holonomy is
reported, and no reusable raw trajectory archive or body-action calibration
was located.

The candidate-specific audit scores this packet `6/13`. Its published control
metrics support first-law, feedback/open-loop and projective-endpoint checks;
the primary `x_A=x_Q`, holonomy and QOR scores remain programmatically
forbidden.

The Cottet Maxwell-demon packet is the strongest independently instrumented
energetic control and scores `7/13`: output work is measured directly and
cross-checked against qubit energy and memory tomography. However, the
input--output law makes its conditional mean a function of the reconstructed
qubit state and frozen controls. It is therefore instrument-independent but
not a descriptor-novel action axis. It also has no frozen map to
`A_X^PC/A_*`, closed holonomy or raw archive. Cottet and Naghiloo have
complementary control roles and must not be stitched across carriers.

The fluctuation audit preserves one qualified opening. Multi-time output
correlations can add dynamical information beyond instantaneous tomography,
but input--output theory and quantum regression determine them once the full
Markov generator, couplings, inputs and controls are independently frozen.
Cottet provides no reusable single-shot archive for testing residual novelty,
so this opening receives no score and cannot be identified with body action.

A finite public-data audit located one genuinely reusable single-shot packet:
Wang et al.'s Zenodo IQ records. Three prepared-state datasets contain about
29,400 complex samples per class and give frozen midpoint-classification
accuracies of `99.51%`, `99.56%` and `99.80%`. This validates the executable
readout-control pipeline, but the samples are integrated endpoints rather than
time trajectories and contain no independent action or holonomy leg. Ficheux
has the strongest process architecture, including time records and independent
tomography, but its raw records are request-only. Zero directly public packets
therefore pass the full process-residual gate.

A public Dryad optical path-integral packet adds an independent control from a
different platform. Across 100 action bins, path amplitude is consistent with
a constant (weighted slope `-0.0287 +/- 0.0417`), while measured phase follows
the reported action law with RMS residual `0.0218` in units of pi. This
supports the standard Feynman path-amplitude law. The public path-length and
action tables are separate aggregates rather than joint path-level records,
so conditional morphology novelty beyond action cannot be computed and no Tau
body-action score is licensed.

A follow-up source audit found no directly published table carrying geometry,
action, complex amplitude and phase under one path identifier. It did find one
reconstructible packet: the raw Wen polarization-camera matrices determine a
complex propagator, from which the `17^5` lattice-path weights can be rebuilt
as products of measured step propagators. This enables a morphology-beyond-
total-action diagnostic, provided uncertainty is clustered by the shared
propagator elements. The generated paths are not independent measurements,
and their standard optical action is not the Tau body action. Tian et al.'s
companion Dryad packet adds 10,000 measured path-action/action-gradient pairs
per reported condition, but publishes full geometry only for selected paths
and no same-index complex amplitudes.

That reconstruction has now been executed. The radial phase kernel passes the
independent FigS1C check with `0.0120 pi` RMS after one common phase offset.
Across all `1,419,857` paths, path length, increment roughness, turn count,
maximum excursion and occupied step-class count add `Delta R^2=0.1758` beyond
scalar standard action, but this is not unusual under a permutation null on
the 17 shared measured kernel classes (`p=0.144`). The raw-image amplitude
reduction shows a larger apparent increment, but it fails the FigS1C amplitude
validation and is excluded from physical interpretation.

The authors' published amplitude reduction in Figs. S2C--S2D now replaces that
failed image-level estimate. Its 17 normalized radial means vary by only
`0.863%`, but their path products retain `Delta R^2=0.4962` beyond scalar total
action. The shared-kernel permutation gives `p=0.004`; a 9,999-draw
constant-amplitude null propagated through the published histogram errors gives
`p=0.0009`. Replacing any one radial class by its action-trend value leaves at
least `Delta R^2=0.2936`. This is a reproducible standard optical-kernel
morphology signal. It is not a Tau detection because the optical action is not
the independent once-counted body action and all paths reuse the same 17
measured kernel classes.

A source-frozen one-descriptor follow-up separates that broad result from a
Tau-motivated candidate. The tested candidate is the increment-field
Dirichlet energy `sum_k (Delta x[k+1] - Delta x[k])^2`. It adds only
`Delta R^2=0.000405` beyond total action and is null under permutation of the
17 shared kernel classes (`p=0.543`; rank `4/5` among the single-descriptor
controls). By contrast, ordinary path length alone adds `Delta R^2=0.4826`.
The published amplitude structure is therefore dominated by a standard
step-accumulation variable, while this conditional Tau curvature candidate is
not detected. The candidate remains conditional because applying the body
Dirichlet form to increments requires a sourced typed edge carrier.

The separately archived Fig. S2B table strengthens only that standard phase
control. After interpolating its 200-point theory curve onto the 17 measured
radial coordinates, the phase RMSE is `0.01233 pi` and every point lies within
one reported standard deviation (`max |z|=0.4873`). Because the table has no
same-index complex amplitude, recovery record or independent Tau body action,
it does not reopen Tau-primary scoring.

An explicit variable-gap qubit loop now verifies that the three procedures
are jointly compatible on one Hamiltonian family. It produces a nontrivial
work characteristic function, finite endpoint chords and a nonzero Uhlmann
loop phase. This removes protocol incompatibility as a blocker, but it does
not identify terminal quench work with the independent morphological action.

The same model gives a sharper no-go. Gibbs-state dissipated work equals a
quantum-relative-entropy separation, while the Paper-VI target is an
Uhlmann/Bures chord. Their ratio varies along the frozen loop (`2.1171` to
`2.4882`), so no universal gain turns ordinary work into `x_A=x_Q`.

The retained positive bridge is body-side generalized work instead:
`alpha_B=d A_X^PC` is reconstructed from a source-frozen force/stiffness
measurement and integrated from the zero-defect reference before tomography.
This conditionally supplies an independent `x_A`; it is not yet present in the
audited public qubit datasets.

The frozen readiness score ranks the Zhao Xmon architecture first (`8/13`),
followed by the Cottet work/tomography and Viyuela Uhlmann packets (`7/13`).
All fail the independent-body-action hard gate, so the Tau-ready count remains
zero. These are capability scores, not signal significances.

A focused resonator audit closes one tempting shortcut. If a measured cavity
response is a function of the reconstructed state and frozen controls, adding
it to tomography cannot increase conditional Jacobian rank and cannot identify
an independent `x_A`. Six audited readout, measurement-backaction and QGT
implementations fail this independence test. Calorimetry is the one audited
architecture that adds an independent energy-exchange record, but it still
lacks a frozen body-action map and the same-carrier endpoint/holonomy packet.
A resonator remains viable only as a separately calibrated source port whose
response does not factor through the state/QGT record.

The generalized-force shortcut is now closed explicitly. For a controlled
Hamiltonian family, the standard force `M_mu=-Tr[rho d_mu H]` factors through
the reconstructed state and frozen controls. Its nonadiabatic response can
measure Berry curvature or other quantum-geometric data, but this is an
independent instrument only, not an information-independent Tau action axis.
Reopening requires a blind, calibrated source-port residual beyond that
standard force law; calorimetric or mechanical backaction remains a candidate
architecture, not a completed public-data endpoint.

A final bounded public-source-port audit checks the strongest thermal and
mechanical alternatives. The Gunyhó thermal-detector archive is genuinely raw
and single-shot, but its bolometer signal is the qubit's resonator readout
power and therefore another state terminal. Pulsed and collective
optomechanical experiments provide mechanical tomography or force-sensing
capability, but no joint independent applied-force/body-action ordinate and
Uhlmann/QGT endpoint. The audited public-data-only class is therefore closed
with zero eligible packets; further broad candidate collection is not the next
step.

The internal source question is now separated into proof levels. The inherited
CHDF minimum-action quotient already fixes a positive body action and therefore
its exact work form `alpha_B=d A_Q`; symbolic replay verifies closure and path
independence with the frozen radial and tangential stiffnesses. The adopted
PRRC-P1/PJR-P1 working completion carries this body-relative role post-body
without adding a second action or free scale. This does not derive the A8b
connector to the Uhlmann carrier, prove `x_A=x_Q`, show that the unrestricted
physical parent law realizes and occupies
PRRC-P1, or replace the missing empirical source-port measurement.

The finite connector audit sharpens this boundary. For the normalized
Uhlmann convention `x_Q=2(1-sqrt(F))`, an exact symmetric-qubit realization
exists for every occupied action ratio `0 <= x_A <= 2`, with
`r(x_A)^2=x_A-x_A^2/4`. It cannot be a global identity for the unbounded CHDF
quadratic action because `x_Q <= 2`. A continuum of CPTP depolarizing maps
preserves positivity and normalization while changing `x_Q`; generic quantum
conditions therefore do not select the required connector. The remaining
physical statement is the occupation law selecting `V_phys=V_r(x_A)`, or a
separately derived bounded action coordinate.

The existing exact-recovery branch makes this sharper. Within the
depolarizing family, exact CPTP recovery on the full occupied qubit operator
system is possible if and only if `gamma=1`: orthogonal-state trace distance
contracts from one to `gamma`, and no recovery channel can increase it. Thus
full-code recoverability uniquely selects the exact bounded A8b member. What
remains open is not mathematical uniqueness inside this family, but why the
physical base--seed parent imposes exact full-code recoverability.

The parent-realization question is now closed at the current-axiom level by an
exact no-go. The `gamma=1` and `gamma=1/2` depolarizing descents preserve the
same upstream stabilized body, occupied P3 algebra, positivity and CPTP
structure, but only the first has exact full-code recovery and finite A8b.
Thus the current `B_tau^phys+s_U` reduct does not entail the complete quantum
packet. A positive sufficient theorem reduces the missing physics to one
object: an occupied once-counted representation that is action-isometric,
metric--symplectic, separating, monoidal and exactly recoverable on its
protected logical algebra. Physical occupation of this `SFPR_Q` object is not
yet derived.

The physical selector for the recovery part is now sharper. In an isolated
occupied scope, require the parent action and source incidence to factor
between the logical sector and the inaccessible realization complement. The
complementary output is then independent of the logical state. Standard
information--disturbance reversibility gives an exact CPTP recovery, and an
equal-dimensional minimal terminal is unitary. Classical pointer stability is
not enough: dephasing preserves the `|0>,|1>` records while erasing the
`|+>,|->` distinction. The factorized zero-mixed-Hessian law is required by
the observed isolated-system quantum limit, but its first-principles
base--seed derivation remains open.

A future-extension audit adds the missing possibility-space layer. Orientation
and positive stiffness alone leave forward and reversed Hessian balls
isometric. A genuinely larger future possibility space instead requires an
asymmetry of compatible extension fibres: seed/current boundary data constrain
backward completion more strongly than forward completion. The body Hessian
then induces a positive metric on the surviving forward fibre. This is still
classical unless the same source also supplies an antisymmetric phase
curvature satisfying the metric--symplectic lock. Observer readout accesses a
descriptor of this extension fibre, not actual future outcomes, so the
construction does not authorize retrocausal signalling.

The phase input can now be narrowed further. For one differentiable ordered
parent action, its bulk second variation supplies the Jacobi/stiffness metric
on compatible future extensions, while the antisymmetrized variation of its
boundary potential supplies the presymplectic phase form on the observer cut.
After quotienting gauge/null directions, a nondegenerate phase form and the
positive metric canonically determine a complex structure by polar
decomposition. Thus a separate fitted phase source is unnecessary. The same
argument does not force all polar mode scales to agree, so one universal
action unit remains an explicit physical-realization condition.

That relative-scale condition is now closed under one additional, sharply
typed hypothesis. If the occupied future-extension fibre is irreducible under
a parent symmetry preserving both the metric and phase form, then the positive
polar operator commutes with that symmetry. Its eigenspaces are invariant, so
irreducibility forces it to be `kappa I`. Hence all relative mode scales agree.
The absolute value and SI calibration of `kappa`, and unrestricted physical occupation
of the irreducible orbit, are not derived.

The required irreducible carrier need not be new. The occupied logical ROOT
qubit already carries the real four-dimensional irreducible `SU(2)` module.
A source-owned bijective metric-phase intertwiner from the future-extension
fibre to that ROOT amplitude carrier transfers irreducibility and therefore
the scalar lock. The present stabilized-body and internal-ROOT reduct does not
select this bridge: zero-rank and full-rank bridge completions share those
data. The remaining source problem is consequently one typed bridge
occupation, not another free quantum sector.

The bridge source is now localized to the mixed future/ROOT block of the same
parent boundary second variation. Between equivalent irreducible modules any
nonzero equivariant mixed block has invariant kernel and image, hence is
automatically an isomorphism. A separate bijectivity postulate is unnecessary.
The diagonal common-action data still allow both zero and nonzero mixed
blocks, so physical activation of this one incidence remains open. This does
not conflict with logical isolation: the required block loads the protected
ROOT sector from the body possibility fibre, whereas isolation forbids leakage
from ROOT into the inaccessible realization complement.

The existing BRAC morphology potential is not this source: it gives a nonzero
bulk body--field Hessian on occupied fields but, being zeroth order, contributes
no mixed symplectic boundary potential. The selected JCSEL--BRDC--EBRP
completion nevertheless activates the required block through its covariant
kinetic term. Its canonical momentum
`pi_Phi=Z_Phi sqrt(h) n^mu partial_mu Phi` depends on the body metric. An
occupied EBRP mode has nonzero momentum, and any non-gauge future mode changing
the densitized normal metric therefore gives `delta_M pi_Phi != 0`. This proves
conditional activation inside that working completion, not unrestricted
parent-law
selection of the completion.

The future-fluidity intuition now has an exact rank theorem. At one observer
cut, backward-compatible extensions must satisfy both current compatibility
and agreement with already stable observer records, whereas forward extensions
cannot be constrained by unavailable future records. Thus
`R_minus=(R_plus,R_record)`, so `F_minus` embeds in `F_plus` and
`dim(F_plus)-dim(F_minus)=rank(R_record restricted to F_plus)`. Strictly
positive restricted record rank makes the observer's future possibility fibre
larger. This uses simultaneous observer-cut constraints, not parent memory or
access to actual future outcomes. Extra fibre directions become coherent
quantum alternatives only after the phase/ROOT bridge conditions proved above.

"Larger" never means infinite or unconstrained. The physically admissible
future set is the intersection of the future fibre, causal cone, morphology
domain and a finite body-action ellipsoid
`{v:<v,g_plus v><=Lambda}`. With finite fibre rank, positive `g_plus`, finite
`Lambda` and closed constraints this set is compact and finite-volume. The
observer quantizer resolves only finitely many stable cells inside it. Future
fluidity is therefore a relative surplus of body-allowed alternatives over the
more strongly record-constrained past, not unlimited freedom.

Here "full code" means the full occupied logical ROOT operator system, not
the complete morphological-body realization space. A protected-factor audit
shows that observer descent may erase a realization/gauge factor while acting
as the identity on the logical factor. Logical coherence and the A8b
connector are then exactly recoverable although the total descent remains
noninjective and information-losing. In the general isometric formulation,
this requires the complementary output to be independent of the protected
logical state. The remaining physical question is whether the base--seed law
places all ROOT information in such a protected factor.

No additional realization factor is needed once the existing tensor packet
selects a ROOT `M2` factor on the occupied `M8` block. Its full commutant is
canonically `M4`; the two commuting algebras jointly generate all of `M8`.
The protected logical/realization split is therefore derived after one
logical factor has been designated inside the selected Clifford packet. This
designation is not automatic: the rooted `S3` symmetry permutes the three
factors transitively and fixes no individual factor. OHC-P1 plus rooted
TC-CVP1 conditionally selects the unordered oriented factor triple, while a
typed `S3`-breaking source or observer-source access functional is still
required to identify one factor as the logical ROOT algebra. Central
chirality remains a separate discrete selection.

For the natural convex family of observer access maps
`C_w=sum_j w_j Tr_not_j`, a state encoded in factor `j` is seen through a
depolarizing channel with `gamma=w_j`. Therefore a merely dominant factor is
not enough: exact logical recovery and exact A8b select an extremal access
weight `w=e_j`. Symmetric access gives `gamma=1/3`; a generic interior weight
remains irreversible. Different observers may select different vertices
covariantly under factor relabeling. What remains open is whether the physical
observer-access law actually occupies a simplex vertex.

The general terminal-minimality audit sharpens this further. Once a complete
logical `M_2(C)` code is required to have exact CPTP recovery through one
qubit terminal, the channel restricted to that occupied code is necessarily
unitary and therefore factor-pure. What remains open is which factor the
typed observer-source context designates, and why the unrestricted physical
parent law occupies the exact recovery standard-limit completion.

The observer-context selector then identifies the designated factor without
altering the body: when the complementary observer/noise algebra is the full
`M_4(C)` realization algebra, its commutant is one unique logical `M_2(C)`
factor. The three possible observer contexts form one covariant `S3` orbit;
physical occupation of such a maximal complementary algebra remains open.

After a typed factor embedding is supplied, complete `M_8(C)` occupation and
the normalized-trace-preserving bimodular conditional expectation remove that
maximality assumption: the canonical observer map is the partial trace over
`M_4(C)`, whose minimal complement is the full four-dimensional realization
factor. The remaining continuous input is therefore the typed observer-source
incidence selecting the `M_2(C)` embedding itself.

That incidence is already available conditionally from the earlier regular
observer-event theorem on the adopted `E1/EREM` completion. Decomposing its
nonzero event cochain over the three factor sectors gives normalized weights;
exact recovery forces those weights to one axis. The event incidence therefore
designates the factor without a new label. Mixed-factor regular events remain
valid interactions but do not define an exact full-qubit A8b terminal.

Exact recovery is only the zero-error endpoint. If
`||R C-id||_diamond <= eta`, orthogonal-state distinguishability gives
`w_j >= 1-eta` in the weighted access family, so the total off-factor
incidence is at most `eta`. This supplies a quantitative noisy-system version
of factor designation without requiring every physical interaction to be
perfectly recoverable.

The remaining central `Cl_7` chirality sign is not a blocker for this paper's
current quantum terminal. Holding the six flag generators fixed already
generates the full `M_8(C)` terminal algebra, while the two choices of the
seventh generator differ only in the odd central extension. A parity-odd or
Weyl-sensitive future terminal must still derive an orientation lock; the
present result does not declare physical chirality gauge or absent.

## Main Claim

The paper also proves a finite but explicitly layered isolation chain. A
disjoint-source valuation makes the parent action additive and its Hessian
block diagonal across disconnected occupied source components. Exact isolated
logical recovery follows only when the source load, global generator and
occupied state factor across the same split, or when logical non-leakage is
proved independently. Crossing source incidence supplies the allowed local
interaction and leakage boundary.

Observer-relative information loss and finite resolution create a common
accessibility quotient, but do not by themselves imply quantum mechanics. A
complex quantum terminal follows conditionally when one frozen post-body
source packet additionally owns a positive metric, a nondegenerate phase
curvature, their compatible complex structure, positive trace effects,
coherent composition and tensor-local operations.

The packet proves or records:

- a classical/quantum same-quotient no-go;
- the metric-symplectic construction of a finite complex Hilbert carrier;
- an existing-source theorem showing that, inside the adopted enriched
  MVP/P3 handoff, the metric, phase form, complex structure, generator,
  effects and action unit descend from one normalized complex matrix block;
- a finite P3 record-rank theorem: primitive and binary record families are
  incomplete, while complete occupation through ternary Clifford words is
  separating on one irreducible `M_8(C)` block;
- a canonical positive realization by 63 binary spectral instruments, plus an
  exact countermodel showing that Frobenius pointer copying alone has only
  rank eight and cannot supply this atlas;
- an existing-source closure showing that the full S4 effect interval and S5
  square-root terminal automatically realize those sharp instruments inside
  the adopted enriched MVP;
- unitary transport from a commuting self-adjoint generator;
- Born probabilities and the interference cross term on the selected carrier;
- no-signalling under local CPTP operations;
- an exact factorization boundary: one ROOT/pointer factor is insufficient,
  while two source-owned commuting `M_2(C)` factors select the third by
  commutant;
- the distinction between record quantization, decoherence and measurement;
- the exact boundary between observer information loss, von Neumann entropy
  and thermodynamic entropy;
- the Planck action scale as a standard-limit identification, not an SI-value
  prediction.

The concrete common-domain rank audit also fixes a useful negative boundary.
The positive CHDF leaf Hessian has real rank three, so identifying that leaf
alone with the complete future fibre can support bridge rank at most three
(and an even antisymmetric phase rank at most two), not the real rank-four
ROOT qubit. The enriched P3/ROOT packet retains the rank-four target, but the
current source ledger does not yet provide its body-first, source-frozen
restriction matrix `R_plus`. The numerical mixed-block rank is therefore
undefined on the current common domain, rather than measured to be zero.

## Does Not Claim

Observed subsystem factorization is not claimed to prove exact finite parent
additivity. A typed stacked second-jet descent that is faithful on parent
cross-forms can lift vanishing terminal mixed responses only to a zero parent
mixed Hessian. Finite valuation requires all mixed finite-response jets or a
separate global source law; recovery additionally requires global
factorization. The known physical terminal families do not yet provide the
global second-jet stack, and an equal-readout hidden-cross countermodel blocks
universal identifiability without faithfulness.

This paper does not prove that the unrestricted physical parent law realizes
and occupies the required enriched source
packet or the adopted P3 handoff. It does not derive the numerical SI value of
`hbar`, a unique tensor product from the minimal Tau core, physical collapse,
nonperturbative QFT, quantized geometry, the Standard Model, or an observed
deviation from quantum mechanics.

## Inputs From Papers I-V

- Paper I: atemporal body, common observer-access architecture, conditional
  Hilbert/Born leaf and QOR geometry.
- Paper II: finite typed source support and joint-incidence discipline.
- Paper III: complete/recovery-correctable record edges and Uhlmann-distance
  preservation bounds.
- Paper IV: conditional common-source occupation and unrestricted
  parent-realization boundary.
- Paper V: observer-relative stable quantization, common P3 handoff, and the
  calibrated clock needed to write unitary evolution in terminal time.

The manuscript also performs a corpus-wide hub reconciliation. It imports the
existing operator-square/Clifford implication, finite P3 representation,
graph-normal handoff, S4/S5 effect-instrument completion and current-source
typed-incidence no-go at their original claim levels. The remaining
The unrestricted incidence gap is not presented as absence of the lower-level
mathematical construction.

## Common-carrier quantum--gravity boundary

The manuscript now derives the conditional principal-angle description of a
quantum effect and a stress direction on one occupied Hermitian carrier.
Observer coarse graining can reduce the quantum-visible stress component, but
the complex structure alone selects no universal overlap angle. This is an
observer-access theorem, not quantum--gravity detection or terminal identity.

## Full-4D Scoring Compiler Boundary

A shared or mixed parent Hessian is not yet a quantum--gravity excess. The
later joint compiler requires the complete standard comparison
`E_K = (K_HH - K_std) - C K_VV^-1 C^dagger`, one common coframe push, exact
duplicate quotienting, and the full covariance of all remaining terminals.
Paper VI supplies quantum operational obligations but no nonzero physically
occupied full-4D score packet.

## Reproduce

```bash
python3 scripts/reproduce.py
```

The command regenerates the figures and quantum/factorization audit ledgers, builds the PDF
and source-only arXiv archive, and runs the public tests.

## Included Data

The package includes compact frozen public QGT extractions and the six Ness
Figure 2 source tables needed for the QSL control, together with provenance and
derived audit ledgers. No empirical quantum anomaly or Tau detection is
claimed.
